$(document).ready(function () {

    //var sessionRunning;

    //$.getJSON("Api/IsSessionRunning?userid=user", function (data) {
    //    sessionRunning = data.running;
    //});

    //$("#checkinButton").click(function () {
    //    var userTime = $(".time-editor").

    //    if (sessionRunning) {
    //        $.getJSON("Api/CloseSession?userid=user&startTime=time", function (data) {
    //            if (data.success)
    //                sessionRunning = false;
    //        });
    //    } else {
    //        $.getJSON("Api/OpenSession?userid=user&startTime=time", function (data) {
    //            if (data.success)
    //                sessionRunning = true;
    //        });
    //    }
    //});

    //sessionRunning.onchange = function () {

    //};

});